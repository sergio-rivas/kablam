var content = '<%= j render "kablam/message", message: @message %>',
    newMessage = HTMLElement.From(content),
    target = document.getElementById("kablam-chat-window");

taget.prependChild(new_message);
